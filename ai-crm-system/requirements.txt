# --- Web framework ---
fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0

# --- Data / validation ---
pydantic>=2.0
pydantic-settings>=2.0
email-validator>=2.1.0

# --- Database ---
sqlalchemy>=2.0
psycopg2-binary>=2.9.0

# --- LLM / HTTP ---
openai>=1.40.0
python-dotenv>=1.0.0
requests>=2.32.0

# --- Audio transcription ---
# faster-whisper (CTranslate2 backend) is 2-4x faster than openai-whisper,
# uses less RAM, runs well on CPU with int8 quantization, and does not
# require torch. This replaces openai-whisper + torch + torchaudio.
faster-whisper>=1.0.3

# --- Fuzzy matching (CRM entity dedup) ---
# C++-backed string similarity for 'Acme Corp' vs 'Acme Inc' account dedup.
# Zero ML deps, <1 MB wheel. Graceful fallback in mapping_service when missing.
rapidfuzz>=3.6.0

# --- Background jobs ---
APScheduler>=3.10.0,<4.0.0

# --- Google Workspace (optional) ---
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
